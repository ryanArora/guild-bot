{
  "name": "guild-bot",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc --build",
    "clean": "rimraf ./build",
    "dev": "yarn clean && yarn build && node build/index.js"
  },
  "dependencies": {
    "discord.js": "^12.5.1",
    "dotenv": "^8.2.0",
    "mongoose": "^5.11.14"
  },
  "devDependencies": {
    "@types/mongoose": "^5.10.3",
    "@types/node": "^14.14.22",
    "rimraf": "^3.0.2",
    "typescript": "^4.1.3"
  }
}
